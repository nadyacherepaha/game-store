{
    "compilerOptions": { //intellisense config for VSCode
        "baseUrl": "./",
        "paths": { //for sync with aliases in webpack
            "@/": ["src/*"],
            "images/": ["src/assets/images/*"], //it doesn't work with css and images
            "fonts/": ["src/assets/fonts/*"] //it doesn't work with css and images
        },

    }
}